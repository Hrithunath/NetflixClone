import 'package:flutter/material.dart';

const width = SizedBox(
  width: 10,
);
const height = SizedBox(
  width: 10,
);
final BorderRadius radius10 = BorderRadius.circular(10);
 TextStyle homeTiTleText = const TextStyle(fontSize: 14, fontWeight: FontWeight.bold);
